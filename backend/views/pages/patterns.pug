extends ../layout/base.pug
include ../../../frontend/components/core/templates/mixins.pug
include ../../../frontend/components/TabSwitcher/templates/mixins.pug
block content
  section
    h2.h2 Patterns
    p Example of patterns used across great.gov.uk

  section(id="section-layout")
  h2.h2 Layout
  +codeblock('html', 'Header', patterns.header)
  +codeblock('html', 'Footer', patterns.footer)

  section(id="section-typography")
  h2.h2 Typography
  +codeblock('html', 'Typography', patterns.typography)

  section(id="section-partials")
  h2.h2 Page partials

  +codeblock('html', 'Steps', patterns.steps)

  +codeblock('html', 'Steps (on grid)', patterns.stepsOnGrid)

block append foot_scripts
  // home scripts
  script(src="/scripts/pages/home.js")
